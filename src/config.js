
const config = {
  wsEndpoint: process.env.wsEndpoint || 'wss://unique.usetech.com',
  ownerSeed: "tunnel skin series regret outside hockey west strategy west bring daughter actual",
  collectionDataSize: 256,
  collectionId: 13
};

module.exports = config;